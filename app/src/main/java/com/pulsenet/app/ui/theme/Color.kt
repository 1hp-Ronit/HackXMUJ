package com.pulsenet.app.ui.theme

import androidx.compose.ui.graphics.Color

// Flat, restrained dark palette — one accent (SOS red) reserved for emergency
// state, everything else muted so the emergency state actually stands out.
val PulseBackground = Color(0xFF0B0C0E)
val SurfaceDark = Color(0xFF131417)
val SurfaceRaised = Color(0xFF1A1C20)
val BorderSubtle = Color(0x14FFFFFF) // ~8% white
val BorderStrong = Color(0x24FFFFFF) // ~14% white

val SOSRed = Color(0xFFFF3B3B)
val SOSRedMuted = Color(0x1AFF3B3B) // ~10% wash, for backgrounds behind SOS text
val PulseBlue = Color(0xFF3BA7FF)

val TextPrimary = Color(0xFFECEEF0)
val TextDim = Color(0xFF8A8F98)
val TextFaint = Color(0xFF5A5F68)

val MedicalOrange = Color(0xFFFF9F45)
val ResourceYellow = Color(0xFFF5D547)
val GeneralGray = Color(0xFF7A7F88)
